============
Installation
============

At the command line::

    $ pip install dj-chartjs
    $ easy_install dj-chartjs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv venv
    $ pip install dj-chartjs
